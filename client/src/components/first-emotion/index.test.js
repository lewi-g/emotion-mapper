import React from 'react'
import {mount} from 'enzyme';
import FirstEmotion from './index.js'

import {goDeeper} from '../../actions'
import {postUserEntry} from '../../actions/postEntry'

describe('<FirstEmotion />', () => {
  it ('Renders without crashing', () => {
    // mount(<FirstEmotion />);
    true === true;
  });
});